extends layout
block content
    div#notify
        | Notify信息:
        if typeof(error) != 'undefined' && error
            p #{error}
        if typeof(success) != 'undefined' && success
            p #{success}
    div.back
        - var ref = typeof(referer) !== 'undefined' ? referer : '/'
        a(href="#{ref}") 返回